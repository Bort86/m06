import { InputValidationsDirective } from './input-validations.directive';

describe('InputValidationsDirective', () => {
  it('should create an instance', () => {
    const directive = new InputValidationsDirective();
    expect(directive).toBeTruthy();
  });
});
